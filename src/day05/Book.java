package day05;

public class Book {
    static String title; //필드 선언.
    //필드 선언에서는 [접근제한자] + [static] + [final] + 타입 필드명
    int price;

    //생성자 배우기 .
    //문법 : 접근제한자 클래스명(매개변수) {   }
    public Book(String title, int pirce) {
        this.title = title;
        this.price = price;
    }
}
